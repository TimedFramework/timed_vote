CONFIG = {}

-- for server cfg; set vote_token "dein vote token"

CONFIG.Framework = {
    VORP = true,
    TIMED = false -- WIP
}

CONFIG.Items = {
    {id = "lootbox", amount = math.random(1, 3)},
}